apiVersion: v1
kind: Service
metadata:
  name: {{ include "clipper-postgres.fullname" . }}
  labels:
    {{- include "clipper-postgres.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: postgres
      protocol: TCP
      name: postgres
  selector:
    {{- include "clipper-postgres.selectorLabels" . | nindent 4 }}
