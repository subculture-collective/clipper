apiVersion: v1
kind: Service
metadata:
  name: {{ include "clipper-redis.fullname" . }}
  labels:
    {{- include "clipper-redis.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: redis
      protocol: TCP
      name: redis
  selector:
    {{- include "clipper-redis.selectorLabels" . | nindent 4 }}
